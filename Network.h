// Adjust the following values to match your needs
// -----------------------------------------------
#define   servername "IS"     // Set your server's logical name here e.g. if myserver then address is http://myserver.local/
IPAddress local_IP(192, 168, 254, 120); // Set your server's fixed IP address here
IPAddress gateway(192, 168, 254, 254);    // Set your network Gateway usually your Router base address
IPAddress subnet(255, 255, 255, 0);   // Set your network sub-network mask here
IPAddress dns(192,168,254,254);           // Set your network DNS usually your Router base address
const char ssid_1[]     = "PowerOfPhysics";
const char password_1[] = "siJonasBakling";

const char ssid_2[]     = "PowerOfPhysics_plus";
const char password_2[] = "siJonasBakling";

const char ssid_3[]     = "your_SSID3";
const char password_3[] = "your_PASSWORD_for SSID3";

const char ssid_4[]     = "your_SSID4";
const char password_4[] = "your_PASSWORD_for SSID4";
